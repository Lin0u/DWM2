<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Canje de Puntos - FitExpress</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="css/style.css">
    <style>
        /* Estilos específicos para la sección de canje */
        .points-box {
            background-color: var(--color-card-dark);
            border: 2px solid var(--color-border-accent);
            border-radius: 12px;
            padding: 20px;
            text-align: center;
        }
        .points-box h2 {
            font-size: 3rem;
            color: var(--color-border-accent);
            font-weight: 800;
        }
        .points-cost {
            font-size: 1.2rem;
            font-weight: bold;
            color: var(--color-text-light);
            margin-top: 10px;
        }
        .card-redeem .card-body {
            text-align: center;
        }
        /* Color para indicar que se puede canjear */
        .canjeable {
            border: 2px solid green !important;
            box-shadow: 0 0 10px rgba(0, 255, 0, 0.3);
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-expand-lg sticky-top">
        <div class="container-fluid container">
            <a class="navbar-brand logo" href="index.html">🥗 FitExpress</a>
            <div class="d-flex">
                <a class="nav-link text-light me-3" href="perfil.html">👤 Perfil</a>
                <a class="nav-link text-light" href="index.html">🏠 Menú</a>
            </div>
        </div>
    </nav>

    <div class="container my-5">
        <h1 class="text-center mb-5 section-title mx-auto"> Canjea tus Puntos Fit</h1>

        <div class="row justify-content-center mb-5">
            <div class="col-md-6">
                <div class="points-box">
                    <h5 class="text-secondary mb-2">Tu Saldo Total de Puntos</h5>
                    <h2 id="current-points">10.000</h2>
                    <p class="text-success fw-bold">¡Tienes puntos disponibles para canje!</p>
                </div>
            </div>
        </div>
        
        <h3 class="text-light text-center mb-4">Bowls Disponibles para Canjear</h3>
        <div class="row row-cols-1 row-cols-md-3 g-4" id="redeem-options">
            </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="js/main.js"></script>
    <script>
        // Los bowls y sus costos en puntos (Simulación)
        const redeemableProducts = [
            { id: "b1", name: "Bowl Pollo Teriyaki", image: "img/Bowl Pollo.jpg", points: 8000 },
            { id: "b3", name: "Bowl Ensalada César", image: "img/Bowl Ensalada.jpg", points: 7000 },
            { id: "b2", name: "Bowl Lomo Saltado Fit", image: "img/Bowl Carne.jpg", points: 9500 }
        ];

        // Simulación del saldo del usuario (ajusta este valor para probar)
        const userPoints = 10000; 

        // Función para renderizar la tarjeta de canje
        function renderRedeemCard(product) {
            const canAfford = userPoints >= product.points;
            const cardClass = canAfford ? 'canjeable' : 'text-opacity-50'; // Añade borde verde si alcanza

            return `
                <div class="col">
                    <div class="card card-bowl card-redeem h-100 ${cardClass}">
                        <img src="${product.image}" class="card-img-top" alt="${product.name}">
                        <div class="card-body">
                            <h5 class="card-title">${product.name}</h5>
                            <p class="points-cost">${product.points.toLocaleString('es-CL')} Puntos</p>
                            <button 
                                class="btn btn-lg mt-2 ${canAfford ? 'btn-accent' : 'btn-secondary'}" 
                                ${canAfford ? `onclick="attemptRedeem('${product.id}', ${product.points})"` : 'disabled'}
                            >
                                Canjear
                            </button>
                            ${!canAfford ? '<p class="text-danger small mt-2">Puntos Insuficientes</p>' : ''}
                        </div>
                    </div>
                </div>
            `;
        }

        // Función para simular el intento de canje
        function attemptRedeem(productId, pointsCost) {
            // Verifica nuevamente si los puntos son suficientes (solo por seguridad)
            if (userPoints < pointsCost) {
                alert('❌ Puntos Insuficientes. Necesitas ' + pointsCost.toLocaleString('es-CL') + ' puntos.');
                return;
            }

            // Simulación de API de Canje: 70% de éxito
            const success = Math.random() > 0.3; 
            const productName = redeemableProducts.find(p => p.id === productId).name;

            if (success) {
                alert(`✅ ¡Canje Exitoso! Has obtenido el ${productName}. Se ha agregado a tu carrito (o a tus pedidos pendientes) y tus puntos se han reducido.`);
                // Aquí iría la lógica real: restar puntos y registrar el pedido
                // Para la simulación, simplemente recargamos
                // window.location.reload(); 
            } else {
                alert('❌ Error en el canje. Por favor, intenta de nuevo más tarde.');
            }
        }

        // Inicialización
        document.addEventListener('DOMContentLoaded', () => {
            document.getElementById('current-points').textContent = userPoints.toLocaleString('es-CL');
            
            const redeemContainer = document.getElementById('redeem-options');
            redeemContainer.innerHTML = redeemableProducts.map(renderRedeemCard).join('');
        });
    </script>
</body>
</html>
